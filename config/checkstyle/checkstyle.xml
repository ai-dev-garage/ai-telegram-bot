<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "http://www.puppycrawl.com/dtds/configuration_1_3.dtd">
<module name="Checker">
    <!-- ============================================= -->
    <!-- File-level checks                           -->
    <!-- ============================================= -->
    
    <!-- Line length check -->
    <module name="LineLength">
        <property name="max" value="160"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>
    
    <!-- File length check -->
    <module name="FileLength">
        <property name="severity" value="warning"/>
        <property name="max" value="500"/>
        <message key="maxLen.file" value="File length is {0,number,integer} lines (max allowed is {1,number,integer}). Consider refactoring into smaller, more focused classes."/>
    </module>
    
    <!-- File tab character check -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
        <message key="containsTab" value="Line contains a tab character. Use 4 spaces for indentation instead."/>
    </module>
    
    <!-- Trailing spaces -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="message" value="Line has trailing spaces."/>
    </module>

    <!-- ============================================= -->
    <!-- TreeWalker-based checks                      -->
    <!-- ============================================= -->
    <module name="TreeWalker">
        
        <!-- ================================== -->
        <!-- Indentation (4 spaces)            -->
        <!-- ================================== -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="lineWrappingIndentation" value="4"/>
            <property name="arrayInitIndent" value="4"/>
        </module>
        
        <!-- ================================== -->
        <!-- Import checks                      -->
        <!-- ================================== -->
        <module name="AvoidStarImport">
            <property name="severity" value="warning"/>
        </module>
        
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>
        <module name="IllegalImport"/>
        
        <!-- Import order -->
        <module name="ImportOrder">
            <property name="groups" value="com.ai.dev.garage,org,com,jakarta,/^(java|javax)\./,*,lombok"/>
            <property name="ordered" value="true"/>
            <property name="separated" value="true"/>
            <property name="option" value="bottom"/>
            <property name="sortStaticImportsAlphabetically" value="true"/>
        </module>
        
        <!-- Javadoc checks disabled for this project (optional docs only). -->

        <!-- ================================== -->
        <!-- Naming conventions                 -->
        <!-- ================================== -->
        <module name="TypeName">
            <property name="tokens" value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, ANNOTATION_DEF"/>
            <message key="name.invalidPattern"
                     value="Type name ''{0}'' must match pattern ''{1}''."/>
        </module>
        <module name="MemberName"/>
        <module name="LocalVariableName"/>
        <module name="ConstantName"/>
        <module name="StaticVariableName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="PackageName"/>
        
        <!-- ================================== -->
        <!-- Class design                       -->
        <!-- ================================== -->
        <module name="OneTopLevelClass"/>
        <module name="FinalClass"/>
        <module name="HideUtilityClassConstructor"/>
        <module name="InterfaceIsType"/>
        <module name="VisibilityModifier">
            <property name="packageAllowed" value="true"/>
            <property name="protectedAllowed" value="true"/>
            <property name="allowPublicFinalFields" value="true"/>
            <property name="allowPublicImmutableFields" value="true"/>
        </module>
        
        <!-- Group overloaded methods together -->
        <module name="OverloadMethodsDeclarationOrder"/>

        <!-- ================================== -->
        <!-- Method design                      -->
        <!-- ================================== -->
        
        <!-- Parameter number check -->
        <module name="ParameterNumber">
            <property name="severity" value="warning"/>
            <property name="max" value="5"/>
            <property name="tokens" value="METHOD_DEF, CTOR_DEF"/>
        </module>
        
        <!-- Method length -->
        <module name="MethodLength">
            <property name="severity" value="warning"/>
            <property name="max" value="50"/>
            <property name="countEmpty" value="false"/>
        </module>

        <!-- ================================== -->
        <!-- Code structure                     -->
        <!-- ================================== -->
        <module name="NeedBraces">
            <property name="tokens"
                      value="LITERAL_DO, LITERAL_ELSE, LITERAL_FOR, LITERAL_IF, LITERAL_WHILE"/>
        </module>

        <module name="LeftCurly">
            <property name="tokens"
                      value="ANNOTATION_DEF, CLASS_DEF, CTOR_DEF, ENUM_CONSTANT_DEF, ENUM_DEF,
                    INTERFACE_DEF, LITERAL_CASE, LITERAL_CATCH, LITERAL_DEFAULT,
                    LITERAL_DO, LITERAL_ELSE, LITERAL_FINALLY, LITERAL_FOR, LITERAL_IF,
                    LITERAL_SWITCH, LITERAL_SYNCHRONIZED, LITERAL_TRY, LITERAL_WHILE, METHOD_DEF,
                    OBJBLOCK, STATIC_INIT"/>
        </module>
        
        <module name="RightCurly">
            <property name="tokens"
                      value="LITERAL_TRY, LITERAL_CATCH, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE"/>
        </module>

        <!-- ================================== -->
        <!-- Whitespace                         -->
        <!-- ================================== -->
        <module name="WhitespaceAfter">
            <property name="tokens"
                      value="COMMA, SEMI, TYPECAST, LITERAL_IF, LITERAL_ELSE, LITERAL_RETURN,
                    LITERAL_WHILE, LITERAL_DO, LITERAL_FOR, LITERAL_FINALLY, DO_WHILE, ELLIPSIS,
                    LITERAL_SWITCH, LITERAL_SYNCHRONIZED, LITERAL_TRY, LITERAL_CATCH"/>
        </module>
        
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyLambdas" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
            <property name="allowEmptyLoops" value="true"/>
        </module>
        
        <module name="NoWhitespaceAfter">
            <property name="tokens" value="ARRAY_INIT, AT, INC, DEC, UNARY_MINUS, UNARY_PLUS, BNOT, LNOT, DOT, ARRAY_DECLARATOR, INDEX_OP"/>
        </module>
        
        <!-- No whitespace before most tokens -->
        <module name="NoWhitespaceBefore">
            <property name="tokens" value="COMMA, SEMI, POST_INC, POST_DEC, ELLIPSIS, METHOD_REF"/>
        </module>
        
        <!-- Allow line breaks before DOT for method chaining -->
        <module name="NoWhitespaceBefore">
            <property name="tokens" value="DOT"/>
            <property name="allowLineBreaks" value="true"/>
        </module>

        <!-- ================================== -->
        <!-- Code quality                       -->
        <!-- ================================== -->
        <module name="OneStatementPerLine"/>
        <module name="ModifierOrder"/>
        <module name="EmptyStatement"/>
        <module name="DefaultComesLast"/>
        <module name="FallThrough"/>
        <module name="MultipleVariableDeclarations"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        
        <!-- String equality -->
        <module name="StringLiteralEquality"/>
        
        <!-- Empty blocks -->
        <module name="EmptyBlock">
            <property name="option" value="TEXT"/>
            <property name="tokens" value="LITERAL_TRY, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE, LITERAL_SWITCH"/>
        </module>

        <!-- Complexity: enforced by PMD (CyclomaticComplexity / CognitiveComplexity), not duplicated here. -->

        <!-- ================================== -->
        <!-- Security and best practices       -->
        <!-- ================================== -->
        <module name="CovariantEquals"/>
        <module name="EqualsHashCode"/>
        <module name="NoFinalizer"/>
        <module name="SuperClone"/>
        <module name="SuperFinalize"/>
        <module name="PackageDeclaration"/>
        <module name="DeclarationOrder"/>
        
        <!-- Magic numbers -->
        <module name="MagicNumber">
            <property name="severity" value="warning"/>
            <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
            <property name="ignoreHashCodeMethod" value="true"/>
            <property name="ignoreAnnotation" value="true"/>
            <property name="ignoreFieldDeclaration" value="true"/>
        </module>
        
        <!-- Annotations -->
        <module name="AnnotationLocation">
            <property name="allowSamelineMultipleAnnotations" value="false"/>
            <property name="allowSamelineSingleParameterlessAnnotation" value="false"/>
            <property name="allowSamelineParameterizedAnnotation" value="false"/>
        </module>
        
        <module name="MissingOverride"/>
        <module name="SuppressWarningsHolder"/>

    </module>
    
    <!-- Enable @SuppressWarnings filtering -->
    <module name="SuppressWarningsFilter"/>
    <module name="SuppressionFilter">
        <property name="file" value="${checkstyle.suppressions.file}"/>
    </module>
</module>
